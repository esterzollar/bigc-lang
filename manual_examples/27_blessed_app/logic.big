start doing CalculateStats(score)
    # Business Logic (The Muscle)
    Result = score * 2
    # Set a global variable to prove it worked
    FinalScoreRaw = Result
    print "Logic: Calculated Score is $Result"
end doing
